group 'com.globallogic.orchestrator'

dependencies {
    compile project(':orchestrator-base')

    compile project(':orchestrator-dao')

    compile project(':orchestrator-model')

    compile project(':orchestrator-service')
}
