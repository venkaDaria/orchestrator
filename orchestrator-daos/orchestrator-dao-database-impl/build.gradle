group 'com.globallogic.orchestrator'

dependencies {
    compile project(':orchestrator-daos:orchestrator-dao-api')

    compile project(':orchestrator-connectors:orchestrator-connector-database-impl')

    compile files('src/main/config')
}