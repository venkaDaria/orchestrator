group 'com.globallogic.orchestrator'

dependencies {
    compile project(':orchestrator-base')

    compile project(':orchestrator-dao-api')

    compile project(':orchestrator-connector-filesystem-impl')

    testCompile group: 'junit', name: 'junit'
}